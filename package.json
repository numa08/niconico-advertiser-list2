{
  "name": "niconico-advertiser-list2",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "pnpm --filter niconico-advertiser-list2-workers dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "lint": "biome check .",
    "format": "biome format --write .",
    "ci:build": "pnpm typecheck && pnpm test && pnpm -C frontend build"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6"
  }
}
