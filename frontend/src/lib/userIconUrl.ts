// 投稿者アイコンURLの組み立て（仕様: FRONTEND_REQUIREMENTS.md FE-016、付録D-8/E）
// TODO: TDDレッドフェーズのスタブ。テスト(test/userIconUrl.test.ts)を満たす実装に置き換える

/** 投稿者IDからアイコン画像URLを組み立てる。prefixは floor(userId / 10000) */
export function userIconUrl(userId: string): string {
  void userId;
  return "";
}
